const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add('fadeInUp')}})},{threshold:0.5});document.querySelectorAll('.card,.testimonial,.about-grid,.contact-grid').forEach((element)=>{observer.observe(element)});const nav=document.querySelector('nav');const hamburger=document.querySelector('.hamburger');let scrollPosition=0;window.addEventListener('scroll',()=>{const currentScrollPosition=window.scrollY;if(currentScrollPosition>80){nav.style.backdropFilter='blur(10px)'}else{nav.style.backdropFilter='none'}if(currentScrollPosition>scrollPosition){nav.style.top='-100px'}else{nav.style.top='0px'}scrollPosition=currentScrollPosition;});hamburger.addEventListener('click',()=>{nav.classList.toggle('show')});const form=document.querySelector('form');form.addEventListener('submit',(e)=>{e.preventDefault();const formData=new FormData(form);fetch('/submit',{method:'POST',body:formData}).then((response)=>response.json()).then((data)=>console.log(data)).catch((error)=>console.error(error));form.reset();});const stats=document.querySelectorAll('.stats p');stats.forEach((stat)=>{let count=0;const updateCount=()=>{count+=1;stat.textContent=`${count}`;if(count>=100){clearInterval(interval)}};const interval=setInterval(updateCount,10)});