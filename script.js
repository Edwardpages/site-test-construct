const nav = document.querySelector('.nav');const hamburger = document.querySelector('.hamburger');const navLinks = document.querySelectorAll('.nav-links a');const sections = document.querySelectorAll('section');const observer = new IntersectionObserver((entries) => {entries.forEach((entry) => {if (entry.isIntersecting) {entry.target.classList.add('fadeInUp');}})}, {threshold: 0.5});sections.forEach((section) => {observer.observe(section);});hamburger.addEventListener('click', () => {nav.classList.toggle('show');});navLinks.forEach((link) => {link.addEventListener('click', () => {nav.classList.remove('show');});});window.addEventListener('scroll', () => {if (window.scrollY > 80) {nav.style.backdropFilter = 'blur(10px)';} else {nav.style.backdropFilter = 'none';}});const form = document.querySelector('form');form.addEventListener('submit', (e) => {e.preventDefault();const name = document.querySelector('input[type="text"]');const email = document.querySelector('input[type="email"]');const message = document.querySelector('textarea');if (name.value && email.value && message.value) {console.log('Form submitted');form.reset();} else {console.log('Please fill out all fields');}});const stats = document.querySelectorAll('.about-stats p');stats.forEach((stat) => {let count = 0;const updateCount = () => {count++;stat.textContent = count;if (count < 2026) {requestAnimationFrame(updateCount);}};updateCount();});